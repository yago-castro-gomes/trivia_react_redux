const INITIAL_STATE = {
  name: '',
  assertions: '',
  score: '',
  gravatarEmail: '',
};

const reduce = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default: {
    return state;
  }
  }
};

export default reduce;
